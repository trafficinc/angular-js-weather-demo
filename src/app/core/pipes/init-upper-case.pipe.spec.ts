import { InitUpperCasePipe } from './init-upper-case.pipe';

describe('InitUpperCasePipe', () => {
  it('create an instance', () => {
    const pipe = new InitUpperCasePipe();
    expect(pipe).toBeTruthy();
  });
});
